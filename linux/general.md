# General misc stuff that is not limited to linux distro
## Change login shell

So you want to change ht default shell on the system. The command to use is chsh. 

```
$ chsh -s /bin/bash username
```
